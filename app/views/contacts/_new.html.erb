      <%= form_for Contact.new do |f| %>  
      <div class="field"> 
        <% f.label :contact %><br> 
        <%= f.text_area :content %> 
      </div> 
      <div class="actions"> 
        <%= f.submit "Create" %> 
      </div> 
  <% end %>